!!! 5
%html
%head
  %title
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
  = javascript_include_tag 'application', 'data-turbolinks-track' => true
  = csrf_meta_tags
%body
  %nav.navbar.navbar-default
    .container
      .navbar-brand= link_to "Pin Board I do", root_path

      - if user_signed_in?
        %ul.nav.navbar-nav.navbar-right
          %li= link_to "Nouveau Pin", new_pin_path
          %li= link_to "Compte", edit_user_registration_path
          %li= link_to "Déconnexion", destroy_user_session_path, method: :delete
      -else
        %ul.nav.navbar-nav.navbar-right
          %li= link_to "s'inscrire", new_user_registration_path
          %li= link_to "se connecter", user_session_path

.container
- flash.each do |name, msg|
  = content_tag :div, msg, class: "alert-info"

= yield
